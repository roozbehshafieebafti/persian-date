import { getFull } from '../../get-full/index';

describe('test group of getFull',()=>{
    it('should exist',()=>{
        expect(getFull).toBeDefined();
    });

    it('functional test', () => {
        
    });
})