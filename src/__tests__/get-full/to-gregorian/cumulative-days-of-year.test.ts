import { cumulative } from '../../../get-full/to-gregorian/cumulative-day-of-year';

describe('test cumulative part', () => {
      it('should exist', () => {
            expect(cumulative).toBeDefined();
      });
});
